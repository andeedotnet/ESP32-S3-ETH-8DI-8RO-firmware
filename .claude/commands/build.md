---
description: Build ESP-IDF firmware (idf.py build)
---

Source the ESP-IDF environment and build the firmware. Run this command and show the result:

```bash
source ~/.espressif/v6.0.1/esp-idf/export.sh && idf.py build
```
